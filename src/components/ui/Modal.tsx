import { useEffect } from "react";
import { X } from "lucide-react";
import type { ReactNode } from "react";
type ModalProps = { open: boolean; onClose: () => void; title: string; description?: string; children: ReactNode; footer?: ReactNode; width?: number | string; closeOnBackdrop?: boolean };
export function Modal({ open, onClose, title, description, children, footer, width, closeOnBackdrop = true }: ModalProps) { useEffect(() => { if (!open) return; const closeOnEscape = (event: KeyboardEvent) => { if (event.key === "Escape") onClose(); }; window.addEventListener("keydown", closeOnEscape); return () => window.removeEventListener("keydown", closeOnEscape); }, [open, onClose]); if (!open) return null; return <div className="kv-modal-backdrop" role="presentation" onMouseDown={() => closeOnBackdrop && onClose()}><section className="kv-modal" role="dialog" aria-modal="true" aria-label={title} style={width ? { "--kv-modal-width": typeof width === "number" ? `${width}px` : width } as React.CSSProperties : undefined} onMouseDown={(event) => event.stopPropagation()}><header className="kv-modal__header"><div><h2 className="kv-modal__title">{title}</h2>{description && <p className="kv-modal__description">{description}</p>}</div><button type="button" className="kv-modal__close" onClick={onClose} aria-label="Close dialog"><X size={18}/></button></header><div className="kv-modal__body">{children}</div>{footer && <footer className="kv-modal__footer">{footer}</footer>}</section></div>; }
